---
description: Show remaining YouTube API quota and what it buys today
---

Run `ytps quota` and explain the numbers in plain terms:

- how many song-adds remain today (each costs 50 units),
- when the quota resets (midnight US/Pacific),
- and if the user has a pending publish job, whether it fits in what is left.

If it does not fit, say so directly and point at `ytps publish links`, which costs
no quota at all.
